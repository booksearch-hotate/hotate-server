<%#
本リクエストの確認画面の各項目に共通するUI

@params {title: string, mes: string}
%>
<%
let mes = content;

if (mes === '' || mes === null) mes = 'なし';
%> 
<div class="confirm-item-box">
  <p class="title mb-0"><%= title %></p>
  <p class="content" style="font-size: 1.5rem;"><%= mes %></p>
</div>