<%#
各ページの共通のヘッダ

@params {mes: string, logo: string}
%>
<div class="line-title">
  <%= mes %>
  <% if (typeof logo !== 'undefined' && logo.length !== 0) { %>
    <i class="bi bi-<%= logo %>"></i>
  <% } %>
</div>