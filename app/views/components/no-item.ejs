<%#
アイテムがない場合に表示するUI
%>
<div class="text-center">
  <% if (typeof icon !== 'undefined') { %>
    <div style="height: 6rem;"><%- include(`./../icons/${icon}`) %></div>
  <% } %>
  <h3 class="text-danger mt-2"><%= title %></h3>
  <p><%= mes %></p>
</div>